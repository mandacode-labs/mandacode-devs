---
import AdminLayout from "@/components/layouts/AdminLayout.astro";
import AdminSidebar from "@/components/admin/AdminSidebar.astro";
import AdminHeader from "@/components/admin/AdminHeader.astro";
import AdminCard from "@/components/admin/AdminCard.astro";

interface Props {
  title: string;
  headerTitle: string;
  headerSubtitle?: string;
  backHref: string;
  backLabel: string;
  currentPath: string;
}

const { title, headerTitle, headerSubtitle, backHref, backLabel, currentPath } =
  Astro.props;
---

<AdminLayout title={title}>
  <div class="flex min-h-screen">
    <AdminSidebar currentPath={currentPath} />

    <div class="flex-1 md:ml-64">
      <AdminHeader title={headerTitle} subtitle={headerSubtitle} />

      <main class="p-4 sm:p-6 lg:p-8">
        <div class="max-w-4xl mx-auto">
          <div class="mb-6">
            <a
              href={backHref}
              class="inline-flex items-center gap-1 text-sm text-text-secondary hover:text-text-primary transition-colors"
            >
              <svg
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                class="w-4 h-4"
              >
                <path d="M19 12H5M12 19l-7-7 7-7"></path>
              </svg>
              {backLabel}
            </a>
          </div>

          <AdminCard class="p-6">
            <slot />
          </AdminCard>
        </div>
      </main>
    </div>
  </div>
</AdminLayout>
