---
interface Props {
  variant?: "primary" | "secondary" | "ghost";
  href?: string;
  class?: string;
  onclick?: string;
  target?: string;
  rel?: string;
}

const {
  variant = "primary",
  href,
  class: className = "",
  onclick,
  target,
  rel,
} = Astro.props;

const baseClasses =
  "inline-flex items-center justify-center gap-2 px-4 py-2 text-sm font-medium rounded-lg transition-colors";

const variants = {
  primary: "bg-accent-subtle text-accent hover:bg-accent hover:text-white",
  secondary:
    "bg-bg-secondary text-text-primary border border-border hover:bg-bg-tertiary",
  ghost: "text-text-secondary hover:text-text-primary hover:bg-bg-secondary",
};

const classes = `${baseClasses} ${variants[variant]} ${className}`;
---

{
  href ? (
    <a href={href} class={classes} onclick={onclick} target={target} rel={rel}>
      <slot />
    </a>
  ) : (
    <button type="button" class={classes} onclick={onclick}>
      <slot />
    </button>
  )
}
