---
import { renderTiptapJson } from "@/lib/tiptap/render";

interface Props {
  content: string;
}

const { content } = Astro.props;
const html = renderTiptapJson(content);
---

<div class="prose prose-neutral max-w-none" set:html={html} />
